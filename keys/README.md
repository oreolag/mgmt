Add the public SSH keys for users you want to create on remote servers with `login_useradd` collection playbook. Name each file after the username, for example `javier.pub`:

```bash
cp ~/.ssh/id_ed25519.pub keys/javier.pub
```
